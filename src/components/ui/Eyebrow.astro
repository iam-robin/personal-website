---
/**
 * Small mono kicker line above a heading
 * (e.g. "Robin Spielmann — Cologne").
 */
interface Props {
    class?: string;
}

const { class: className = "" } = Astro.props;
---

<p class={`font-mono text-sm text-ink-muted ${className}`}>
    <slot />
</p>
