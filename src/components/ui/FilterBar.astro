---
/**
 * Wrapping nav for a row of FilterTag pills. Replaces the near-identical
 * `<nav class="flex flex-wrap …">` containers each subpage filter used to
 * define inline.
 */
---

<nav class="flex flex-wrap gap-2 font-mono text-sm">
    <slot />
</nav>
