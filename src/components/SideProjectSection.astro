---
import { Icon } from 'astro-icon/components';
import { projects } from 'src/content/projects';
import Slider from './slider/Slider.astro';
import SliderItem from './slider/SliderItem.astro';
import ProjectItem from './ProjectItem.astro';
import SectionContent from './SectionContent.astro';
---

<div>
    <SectionContent title="Side Projects">
        Like most of us – I have lot of ideas and projects that I would like to implement. Many of
        them get lost in my notes or as random thoughts on my computer. But can you believe it? Some
        of them actually happen.
    </SectionContent>
    <Slider>
        {
            projects.map((project, index) => (
                <SliderItem classes="py-3" size="medium">
                    <ProjectItem
                        bgColor={project.bgColor}
                        href={project.href}
                        title={project.title}
                        description={project.description}
                        labels={project.labels}
                        year={project.year}
                        logo={project.logo}
                        logoWidth={project.logoWidth}
                        customOpenString={project.customOpenString}
                        comingSoon={project.comingSoon}
                    />
                </SliderItem>
            ))
        }
        <SliderItem>
            <a
                href="https://read.cv/iamrobin"
                class="group flex aspect-square w-full items-end rounded-xl border-2 border-dashed border-black-200 p-6 transition-all duration-300 hover:bg-neutral-100"
            >
                <div class="text-sm">
                    <p>Enough fun side projects –</p>
                    <p>explore what I do for a living</p>
                    <div class="mt-2 flex items-center gap-1">
                        <span>read.cv</span>
                        <Icon
                            name="ph:arrow-up-right-bold"
                            width={16}
                            class="text-neutral-400 transition-all group-hover:translate-x-0.5 group-hover:text-black"
                        />
                    </div>
                </div>
            </a>
        </SliderItem>
        <SliderItem emptyEnd />
    </Slider>
</div>
