name: Daily Workflow

on:
    workflow_dispatch: # Manual trigger
    schedule:
        # Runs at 00:00 UTC every day
        - cron: '0 0 * * *'

jobs:
    daily-job:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout repository
              uses: actions/checkout@v3

            - name: Run your script or task
              run: echo "This job runs once a day"
