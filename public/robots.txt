# Allow all crawlers
User-agent: *
Allow: /

# Utility and placeholder routes use `noindex` on the pages themselves rather
# than a Disallow here. A disallowed URL is never fetched, so the noindex would
# never be read, and the URL could still be indexed bare if anything linked to it.

Sitemap: https://iamrob.in/sitemap-index.xml
